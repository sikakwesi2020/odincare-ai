"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GenerateWidgetSession = void 0;
const Helpers_1 = require("./Helpers");
function GenerateWidgetSession(devId, apiKey, referenceId, language, providers, authSuccessRedirectUrl, authFailureRedirectUrl, showDisconnect) {
    const raw = JSON.stringify({
        reference_id: referenceId,
        providers: providers ? providers.join(',') : undefined,
        auth_success_redirect_url: authSuccessRedirectUrl,
        auth_failure_redirect_url: authFailureRedirectUrl,
        language,
        show_disconnect: showDisconnect,
    });
    const requestOptions = {
        method: 'POST',
        headers: {
            'X-API-Key': apiKey,
            'dev-id': devId,
            'Content-Type': 'application/json',
        },
        body: raw,
    };
    return (0, Helpers_1.RequestWrapper)('auth/generateWidgetSession', requestOptions);
}
exports.GenerateWidgetSession = GenerateWidgetSession;
