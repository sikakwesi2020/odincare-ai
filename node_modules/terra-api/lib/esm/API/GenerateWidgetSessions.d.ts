export interface TerraWidgetResponse {
    session_id: string;
    status: string;
    url: string;
}
export declare function GenerateWidgetSession(devId: string, apiKey: string, referenceId: string, language: string, providers?: string[], authSuccessRedirectUrl?: string, authFailureRedirectUrl?: string, showDisconnect?: boolean): Promise<TerraWidgetResponse>;
