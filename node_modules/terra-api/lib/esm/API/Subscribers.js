import { RequestWrapper } from './Helpers';
export function GetSubscribers(devId, apiKey) {
    const requestOptions = {
        method: 'GET',
        headers: {
            'X-API-Key': apiKey,
            'dev-id': devId,
            'Content-Type': 'application/json',
        },
    };
    return RequestWrapper('subscriptions', requestOptions);
}
