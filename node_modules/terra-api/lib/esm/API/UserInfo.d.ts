import { TerraUser } from '../models/TerraUser';
export interface TerraUserResponse {
    status: string;
    user: TerraUser;
    is_authenticated: boolean;
}
export declare function GetUser(devId: string, apiKey: string, userID?: string, referenceID?: string): Promise<TerraUserResponse>;
export declare function DeauthUser(devId: string, apiKey: string, userId: string): Promise<void>;
