import { TerraUser } from '../models/TerraUser';
export interface TerraDataResponse<T> {
    data: Array<T>;
    status: string;
    user: TerraUser;
    type: string;
    reference?: string;
}
export declare type dataType = 'activity' | 'body' | 'daily' | 'nutrition' | 'sleep' | 'menstruation';
export declare function GetData<T>(type: dataType, devId: string, apiKey: string, userId: string, startDate: Date, endDate?: Date, toWebhook?: boolean, withSamples?: boolean, retryIfRateLimited?: boolean): Promise<TerraDataResponse<T>>;
