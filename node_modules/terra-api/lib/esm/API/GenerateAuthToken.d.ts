export interface TerraAuthTokenResponse {
    status: string;
    token: string;
    expires_in: number;
}
export declare function GenerateAuthToken(devId: string, apiKey: string): Promise<TerraAuthTokenResponse>;
