import { RequestWrapper } from './Helpers';
export function GetProviders() {
    const requestOptions = {
        method: 'GET',
        headers: {
            'Content-Type': 'application/json',
        },
    };
    return RequestWrapper('integrations', requestOptions);
}
